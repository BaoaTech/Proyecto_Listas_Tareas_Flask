Para usar este codgio se requiere:

1. Crear un directorio donde se van a albergar los archivos

2. Inicializar un ambiente virtual: 

 py -3 -m venv venv 

3. Instalar las sigueintes librerias 

	pip install flask 
	
	pip install mysql-connector-python 
	
	pip install neovim 
	
	pip install 
	
	pip install werkzeug 

	pip install python-dotenv


$env:FLASK_APP = "All" Usar despues de set FLASK_APP=